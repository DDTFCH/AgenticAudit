package llm

// OpenAI-compatible adapter (Ollama, vLLM, LM Studio, OpenRouter, LiteLLM).
// The OpenAIProvider already handles arbitrary base URLs and optional API keys,
// so this file just provides a named constructor that the router recognises.

// NewOpenAICompatibleProvider is declared in openai.go — this file is a
// documentation stub so the package explicitly lists all four adapter types.
